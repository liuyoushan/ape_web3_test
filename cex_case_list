CEX 交易所接口自动化测试用例清单
规则：
- P0：核心接口，简历硬货，必须掌握
- P1：差异化资损场景，面试加分，核心竞争力

==========================================================================
一、账户模块（test_cex_fund_001 ~ 004）
==========================================================================

001.【P0】获取账户余额：查询现货/合约账户余额，校验返回格式与精度
   主流程：调 /api/v3/account，验证 balances 数组存在，每项含 asset/free/locked，精度为字符串可转 float。
   原理：账户余额是所有交易的起点，格式不一致会导致后续接口解析失败。

002.【P0】API密钥权限校验：验证只读/交易/提币等级权限隔离
   主流程：用只读 Key 调下单接口，验证返回错误码；用提币 Key 调提币接口，验证通过。
   原理：API Key 权限分级隔离资产安全第一道防线，防止密钥泄露导致资损。

003.【P0】查询子账户列表：主账户查询所有子账户信息
   主流程：调子账户接口，验证返回列表结构、子账户地址格式正确、数量与预期一致。
   原理：子账户是做市商/量化策略的基础设施，格式错误会导致资产管理混乱。

004.【P0】获取账户信息：全量账户资产概览查询
   主流程：调 /api/v3/account，验证 accountType=SPOT、makerCommission/takerCommission 为非负整数、canTrade 正确。
   原理：账户信息是交易权限和手续费率的权威来源，字段缺失会导致业务逻辑误判。

==========================================================================
二、现货交易模块（test_cex_order_005 ~ 014）
==========================================================================

005.【P0】限价挂单(BUY)：挂限价买单，校验资产冻结
   主流程：调 place_order(side=BUY, type=LIMIT)，取盘口买一×0.99 动态定价（不成交），下单后 USDT locked 增加，断言冻结金额 = price × quantity，撤单后 locked 还原。
   原理：限价买单冻结 quote 资金（USDT），不成交则资金锁在盘口，撤单/成交后解冻。

006.【P0】市价挂单(BUY)：花 20 USDT 市价买入 BTC，即时成交
   主流程：调 place_order(side=BUY, type=MARKET, quote_order_qty=20)，验证 status=FILLED + executedQty > 0 + BTC 余额增加。
   原理：市价买单按卖一价立即撮合，quoteOrderQty 指定花多少钱，返回实际买入量和手续费。

007.【P0】撤销订单(SELL)：挂限价卖单，撤单后冻结资产返还
   主流程：调 place_order(side=SELL, type=LIMIT)，取盘口卖一×1.01 动态定价（不成交），撤单后验证 status=CANCELED + BTC locked 还原。
   原理：限价卖单冻结 base 资金（BTC），撤单后解冻。

008.【P0】订单详情查询：挂买单后查订单详情
   主流程：调 place_order(side=BUY, type=LIMIT) 挂单，再调 get_order_status(orderId) 查询，验证字段与下单时一致。
   原理：订单详情是订单的"身份证"，用于追踪、对账、审计。

009.【P0】历史成交查询：查询历史成交记录（依赖006产生的成交）
   主流程：调 get_trade_history(symbol=BTCUSDT)，验证返回 list + 每项含 id/orderId/price/commission/isBuyer。
   原理：成交记录是最终"对账单"，用于核对资金流水和手续费。

010.【P0】当前挂单查询：挂2笔限价买单，查询所有挂单
   主流程：调 place_order 连挂 2 笔 BUY LIMIT，再调 get_open_orders，验证 list 长度 >= 2 + 每项 status=NEW。
   原理：挂单查询反映当前"排队中"的订单，是撤单前必查。

011.【P0】全撤挂单：挂3笔限价买单，一次性全撤
   主流程：调 place_order 连挂 3 笔 BUY LIMIT，再调 cancel_all_open_orders，验证 list 长度 >= 3 + get_open_orders 清空。
   原理：全撤是风控和止损的快捷方式，确保无遗漏。

012.【P0】市价单成交边界：用测试下单接口校验参数边界
   主流程：调 place_test_order 提交低于 minNotional 的单，验证被拒；合法参数校验通过返回空 dict。
   原理：币安对市价单有最小名义值限制（>=5 USDT），避免粉尘交易。

013.【P0】IOC限价单：立即成交或取消，不挂盘
   主流程：调 place_order(time_in_force=IOC)，挂低于市价的价格，验证状态为 EXPIRED/CANCELED + 无挂单残留。
   原理：IOC 能吃多少吃多少，剩余立即撤销，适用于不想暴露挂单意图的大单拆单场景。

014.【P0】FOK限价单：必须全部成交，否则整单撤销
   主流程：调 place_order(time_in_force=FOK)，挂低于市价的价格，验证状态为 EXPIRED/CANCELED + executedQty=0。
   原理：FOK 必须一次性全部成交，否则整单撤销，适用于精确交易场景。

==========================================================================
三、充币模块（test_cex_fund_015 ~ 017）---------测试网不支持，无法真实调接口
==========================================================================

015.【P0】充值记录查询：查询链上充值历史，校验记录字段
   主流程：调 get_deposit_history(BTC)，验证返回 list，有记录时校验含 txId/amount/coin/status。
   原理：充值记录是用户资产来源的凭证，字段缺失会导致对账失败。

016.【P0】获取充值地址：获取用户专属充值地址，校验地址格式
   主流程：调 get_deposit_address(BTC)，验证地址以 1/3/bc1 开头，重复调用返回相同地址（幂等）。
   原理：交易所为每个用户分配唯一充值地址，重复调用应幂等。

017.【P0】链上状态同步校验：MockChain模拟充值，验证状态一致
   主流程：MockChain 注册充值（amount=0.001），验证状态为 confirmed，推进区块后验证高度增加。
   原理：链上状态同步是充值正确性保障，MockChain 模拟链上事件。

==========================================================================
四、提币模块（test_cex_fund_018 ~ 022）---------测试网不支持，无法真实调接口
==========================================================================

018.【P0】提币提交：用户发起提币申请，校验余额扣减与风控
   主流程：调 submit_withdraw(BTC, 0.0001)，验证 free 减少；提交超阈值金额触发风控拦截。
   原理：提币提交后内部立即扣减余额，大额提币触发风控防止资产外流。

019.【P0】提币进度查询：查询提币链上广播/确认状态
   主流程：提交提币后调 query_withdraw_status(withdraw_id)，验证返回含 status 字段。
   原理：提币状态从 PENDING->BROADCASTED->CONFIRMED，需监听链上确认数变化。

020.【P0】提币撤销：处理中状态可撤销，资金返还账户
   主流程：提交提币后立即调 cancel_withdraw(withdraw_id)，验证 status=CANCELED + free 恢复。
   原理：只有 PENDING/PROCESSING 状态可撤，BROADCASTED 后链上已发出无法撤销。

021.【P0】提币历史查询：查询历史提币记录
   主流程：调 get_withdraw_history(BTC)，验证返回 list + 每项含 id/amount/address/status。
   原理：提币历史是"出金账本"，用于对账和审计。

022.【P0】低Gas提币拦截：设置低于网络要求的Gas，校验拦截
   主流程：提交低 Gas 提币，验证返回错误码 + 余额不变。
   原理：Gas 不足交易会被链上拒绝，交易所应提前拦截。

==========================================================================
五、资金划转模块（test_cex_fund_023 ~ 026）---------测试网不支持，无法真实调接口
==========================================================================

023.【P0】现货->合约划转：资产从现货划转至合约账户
   主流程：调 spot_to_futures(USDT, 1.0, MAIN_UMFUTURE)，验证现货 free 减少 + 合约 free 增加 = 划转金额。
   原理：划转是同币种跨账户移动，不涉及链上交易，即时到账。

024.【P0】合约->现货划转：资产从合约划转至现货账户
   主流程：调 spot_to_futures(USDT, 1.0, UMFUTURE_MAIN)，验证合约 free 减少 + 现货 free 增加 = 划转金额。
   原理：反向划转与正向对称，资金在同一用户的两个账户间流动。

025.【P0】划转历史查询：查询跨账户划转历史
   主流程：调 get_transfer_history(USDT)，验证返回 list + 每项含 timestamp/asset/quantity/type。
   原理：划转历史用于追踪资金在账户间的流动轨迹，是内部审计的核心依据。

026.【P0】划转双边对账：双边余额同步校验，划转金额一致
   主流程：记录划转前后双边余额，验证发送方减少额 = 接收方增加额 = 划转金额（差额 < 1e-8）。
   原理：划转必须保证"发送方减少 = 接收方增加 = 金额"，否则账不平。

==========================================================================
六、风控模块（test_cex_risk_027 ~ 030）
==========================================================================

027.【P0】API Key分级权限：只读/交易/提币权限隔离校验
   主流程：用只读 Key 调下单接口验证返回 403，用交易 Key 调提币验证被拦截，权限隔离完整。
   原理：API Key 权限分级隔离是资产安全第一道防线，防止密钥泄露导致全权限资损。

028.【P0】IP白名单拦截：非白名单IP请求被正确拦截
   主流程：用非白名单 IP 调私有接口，验证返回对应错误码 + 身份验证失败。
   原理：IP 白名单限制 API 访问来源，防止密钥泄露后的异地攻击。

029.【P0】系统自动冻结：异常交易触发系统冻结，出金拦截
   主流程：模拟异常交易模式后调提币接口，验证被拦截 + 账户状态标记为 frozen。
   原理：系统自动冻结是风控的最后一道防线，防止异常资金流出。

030.【P0】大额交易自动审核：超阈值交易触发审核，拆分识别拦截
   主流程：提交超阈值交易，验证返回待审核状态；拆分多笔低于阈值的交易，同样被识别拦截。
   原理：大额交易触发人工审核，拆分是常见绕过手段，系统应能识别"化整为零"的洗钱行为。

==========================================================================
七、差异化资损场景（简历核心竞争力）
==========================================================================

031.【P1】区块回滚充值扣回：模拟充值入账后区块回滚，校验余额正确扣回
   主流程：MockChain 模拟充值入账后 rollback，验证账户 free 减少 + 对账一致。
   原理：区块回滚时交易失效，交易所必须检测并扣回已入账的资金。

032.【P1】重复TxHash充值拦截：同一TxHash重复请求，校验防重复入账机制
   主流程：MockChain 模拟重复 deposit，验证第二次请求被拒绝 + 余额仅加一次。
   原理：防重复入账是交易所基本安全机制。

033.【P1】提币链上失败回滚：内部已扣减但链上失败，校验资金正确退回
   主流程：MockChain 模拟 withdraw failed，验证用户 free 恢复 + 对账一致。
   原理：提币失败时资金必须原路退回，否则用户资产受损。

034.【P1】并发下单不丢单：高并发限价/市价单，验证不丢单、资金安全
   主流程：多线程同时下单 50 笔，验证订单数 = 请求数 + 余额变化 = sum(实际成交额)。
   原理：高并发下系统必须保证原子性，不能出现"钱扣了但订单没生成"的丢单。
