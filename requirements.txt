# ==============================================================================
# 企业级 Web3 测试依赖配置
# ==============================================================================
# 说明：只保留核心依赖，让 pip 自动解决版本兼容性
# 更新方式：定期运行 `pip freeze > requirements.txt` 更新版本

# ------------------------------
# Ape 核心框架
# ------------------------------
eth-ape>=0.8.50
ape-solidity>=0.8.0
ape-alchemy>=0.8.0
ape-polygon>=0.8.0

# ------------------------------
# 测试工具
# ------------------------------
pytest>=8.0.0
allure-pytest>=2.16.0
pyyaml>=6.0.0

# ------------------------------
# 代码质量
# ------------------------------
flake8>=6.0.0

# ------------------------------
# 其他工具
# ------------------------------
web3>=6.0.0