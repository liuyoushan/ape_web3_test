# ================================================================================
# 自定义合约测试数据
# ================================================================================

# 管理员权限测试用例
case_018_admin_permission:
  token_name: "TestToken"           # 测试代币名称
  token_symbol: "TT"                # 测试代币符号

# 全局参数读写测试用例
case_019_global_parameter_rw:
  initial_message: "Hello ApeWorX!" # 初始消息
  first_update_message: "First Update Message" # 第一次更新消息
  second_update_message: "Second Update Message" # 第二次更新消息

# 自定义业务逻辑测试用例
case_020_custom_business_logic:
  amount_in_ether: 200              # 测试金额（ether）

# 暂停恢复测试用例（预留）
case_021_pause_unpause:
  pass

# 黑白名单测试用例（预留）
case_022_blacklist_whitelist:
  pass

# 动态参数更新测试用例（预留）
case_023_dynamic_parameter_update:
  pass

# 外部合约调用测试用例（预留）
case_024_external_contract_call:
  pass

# 自定义异常测试用例（预留）
case_025_custom_error_revert:
  pass
