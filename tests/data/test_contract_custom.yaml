# ================================================================================
# test_contract_custom.py 配套测试数据
# ================================================================================

case_018_admin_permission:
  token_name: "TestToken"
  token_symbol: "TT"

case_019_global_parameter_rw:
  initial_message: "Hello ApeWorX!"
  first_update_message: "First Update Message"
  second_update_message: "Second Update Message"

case_020_custom_business_logic:
  amount_in_ether: 200

case_021_pause_unpause:
  # 预留暂停配置
  pass

case_022_blacklist_whitelist:
  # 预留名单配置
  pass

case_023_dynamic_parameter_update:
  # 预留动态参数配置
  pass

case_024_external_contract_call:
  # 预留外部依赖配置
  pass

case_025_custom_error_revert:
  # 预留异常配置
  pass
